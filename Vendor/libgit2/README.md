# libgit2 para iOS

`libgit2.xcframework` (device + simulador, arm64) gerado por `./build-libgit2.sh`, com
libgit2 1.9.7, HTTPS via SecureTransport, SHA via CommonCrypto, regex e zlib embutidos,
sem SSH. O xcframework fica versionado; `src/` e `build/` não.
